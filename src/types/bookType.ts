export type BookType = {
  title: string;
  author: string;
  genre: string;
  publicationDate: string;
  userId: string;
};

export type BookTypeId = {
  id: string;
  title: string;
  author: string;
  genre: string;
  publicationDate: string;
  userId: string;
};
